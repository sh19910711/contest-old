language: cpp
compiler:
  - clang
  - g++
  - g++-4.7
  - g++-4.8
script:
  - cd ./test
  - make
  - make test-only
  - make clean
install:
  - sudo add-apt-repository --yes ppa:ubuntu-toolchain-r/test
  - sudo apt-get -qq update
  - sudo apt-get -qq install g++-4.7 g++-4.8
